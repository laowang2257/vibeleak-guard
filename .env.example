# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
PRICE_AMOUNT_CENTS=900
CURRENCY=usd
# Use /tmp on serverless hosts if the project is deployed without a database.
REPORT_DATA_DIR=./data

# Stripe Checkout (required for real payments)
STRIPE_SECRET_KEY=sk_test_replace_me
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_replace_me
# Required in production webhook verification; optional for local success-page verification fallback
STRIPE_WEBHOOK_SECRET=whsec_replace_me

# Deployment guard; this project must not auto-deploy unless explicitly true
AGENT_MONEY_AUTODEPLOY_APPROVED=false
VERCEL_TOKEN=
