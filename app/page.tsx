'use client';
import { useState } from 'react';

export default function Home() {
  const [url,setUrl]=useState(''); const [loading,setLoading]=useState(false); const [err,setErr]=useState('');
  async function submit(e: React.FormEvent){e.preventDefault(); setLoading(true); setErr(''); try{const r=await fetch('/api/scan',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({url})}); const j=await r.json(); if(!r.ok) throw new Error(j.error||'Scan failed'); location.href='/report/'+j.id;}catch(e){setErr(e instanceof Error?e.message:'Scan failed')}finally{setLoading(false)}}
  return <main className="wrap"><section className="hero"><span className="badge">Built for the “vibe-coded apps leaking data” wave</span><h1>Find exposed keys before your users — or Reddit — do.</h1><p className="muted">VibeLeak Guard scans a public web app and its client assets for leaked secrets, public source maps, and missing browser hardening headers. Free preview, paid full report.</p></section><div className="grid"><form onSubmit={submit} className="card"><h2>Scan a public app URL</h2><p className="muted">Use only URLs you own or are authorized to test. No login or private data required.</p><input className="input" placeholder="https://your-app.example" value={url} onChange={e=>setUrl(e.target.value)} required/><br/><br/><button className="btn" disabled={loading}>{loading?'Scanning…':'Start free preview'}</button>{err&&<p className="danger">{err}</p>}</form><aside className="card"><h2>Paid unlock</h2><div className="price">$9</div><p className="muted">One-time card payment via Stripe Checkout unlocks full findings, evidence snippets, checked assets, and remediation checklist.</p><ul><li>Secret-pattern detection</li><li>Security-header review</li><li>Source-map exposure check</li><li>JSON local storage for simple self-hosting</li></ul></aside></div><p className="footer">Privacy: scans fetch public URLs only; card data is handled by Stripe, never by this app.</p></main>
}
